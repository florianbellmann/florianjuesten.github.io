import React from 'react';

import Layout from '../components/layout/layout';

const NotFoundPage = () => (
	<Layout>
		<h1>404 not found</h1>
		<p>Page not found. <a aria-label="home" href="/">Back to start.</a></p>
	</Layout>
);

export default NotFoundPage;
